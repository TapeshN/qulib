name: Proposal — Refine

on:
  issue_comment:
    types: [created]

jobs:
  refine:
    if: |
      startsWith(github.event.comment.body, '/refine') &&
      contains(github.event.issue.labels.*.name, 'proposal')
    uses: TapeshN/Tap-Agent/.github/workflows/_reusable-proposal-refine.yml@main
    secrets: inherit

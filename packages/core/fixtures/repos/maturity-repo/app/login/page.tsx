// Fixture route: /login (static, non-interactive shell — the interactive form lives
// in components/LoginForm.tsx so this page does not count toward test-id hygiene).
export default function LoginPage() {
  return <div className="login-page">Sign in</div>;
}

node_modules
packages/*/node_modules
dist
packages/*/dist
.env
qulib.test-auth.config.ts
quilib.test-auth.config.ts
qulib-storage-state.json
.scan-state/
output/
.qulib-baselines/

# Agent / LLM infrastructure — local to dev machine, not published
# CLAUDE.md stays tracked — it holds project rules any contributor or agent needs
AGENTS.md
.cursor/
.github/copilot-instructions.md
.copilot/
.aider*
.continue/
datasets/*
!datasets/golden/
datasets/golden/*
!datasets/golden/manifest.json
# Journey → Cypress generator + health-score golden fixtures (TASK-150-P2)
!datasets/golden/journeys/
!datasets/golden/journeys/**
